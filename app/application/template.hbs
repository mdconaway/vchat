{{#bodywrap-widget}}
    {{outlet}}
    {{#if showModal}}
        {{component modalName model=modalContext controller=this openModal='openModal' closeModal='closeModal' disconnectSocket='disconnectSocket'}}
    {{/if}}
{{/bodywrap-widget}}